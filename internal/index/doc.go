// Package index implements the Pebble-backed metadata projection: the
// transaction-and-document index that maps logical keys to block locations, and
// the pending-upload outbox that tracks sealed blocks awaiting backend upload.
package index
