// Package backend abstracts the durable object-storage backend used for block
// upload, with filesystem and S3 implementations behind a single interface and
// a shared error-classification scheme.
package backend
