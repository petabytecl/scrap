apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: scrapd-ingress
  namespace: scrap
spec:
  podSelector:
    matchLabels:
      app: scrap
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 9090
          protocol: TCP
    - from:
        - podSelector:
            matchLabels:
              app: scrap
      ports:
        - port: 9091
          protocol: TCP
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
          podSelector:
            matchLabels:
              app: otel-collector
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
          podSelector:
            matchLabels:
              app: alloy
      ports:
        - port: 9100
          protocol: TCP
